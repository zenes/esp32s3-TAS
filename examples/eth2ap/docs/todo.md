## LCD/이더넷 안정화 작업 상황 (2026-03-19 종료 시점)

### ✅ 완료된 작업
- [x] **하드웨어 안정성 기준점(Baseline) 확보**: 
  - 모든 주변장치(WiFi, ETH, LCD)를 끈 최소 코드에서 시스템이 안정적으로 작동함을 확인.
  - `platformio.ini` 최적화: `opi_opi` (Octal) 모드 설정 및 CPU 클럭 160MHz 하향 조정으로 전력/신호 안정성 확보.

### 📅 내일 예정 작업 (순차 복구 테스트)
1. **LCD 하드웨어 복구 (Core 1)**: `tft.init()`만 살려서 하드웨어 SPI2 엔진 단독 동작 확인.
2. **멀티태스크 복구 (Core 0)**: `lvglTask`를 다시 활성화하여 렌더링 부하 테스트.
3. **무선 기능 복구**: `WiFi.softAP()`를 켜서 라디오 노이즈 영향 확인.
4. **이더넷 최종 통합**: `SPI2_HOST` 공유 모드로 이더넷 복구 및 최종 안정성 검증.

### ⚠️ 현재 상태
- 현재 `eth2ap.ino`는 **최소 진단용 코드**로 덮어씌워진 상태임.
- 내일 작업 시작 시 이전의 `eth2ap.ino` 백업 또는 로직 복구 필요.

