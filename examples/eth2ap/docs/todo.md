## LCD/이더넷 안정화 작업 상황 (2026-03-20 현재)

### ✅ 완료된 작업
- [x] **하드웨어 안정성 기준점(Baseline) 확보**: `opi_opi` 모드 및 160MHz 클럭 최적화.
- [x] **LCD 진단 로직 개선**: `detectLCD()`에 HW 리셋, DC 제어, MISO 풀업(1MHz) 적용 완료.
- [x] **SPI 핀 맵 확정**: MOSI(3), MISO(1), SCLK(2), CS(4), DC(8), RST(15), BL(16).

### 📅 긴급 디버깅 작업 (오실로스코프 정밀 분석)
1. **SPI 신호 무결성 확인**:
   - [ ] 오실로스코프로 **SCLK, MOSI, MISO** 파형 확인 (실제 데이터 전송 여부).
   - [ ] **Reset(IO15)** 신호가 부팅 시 정상적으로 HIGH를 유지하는지 확인.
   - [ ] **CS(IO4)** 신호가 통신 시 LOW로 떨어지는지 확인.
2. **레지스터 읽기 실패 분석**:
   - [ ] `FF FF FF`가 나오는 원인이 물리적 단선인지, 칩 미응답인지 파형으로 판단.
   - [ ] 필요 시 MISO 내부 풀업 외에 외부 풀업 저항 추가 고려.

### 📅 이후 예정 작업 (순차 복구)
1. **LCD 하드웨어 복구**: SPI 통신 확인 후 `tft.init()` 및 화면 출력 테스트.
2. **멀티태스크 복구**: `lvglTask` 활성화 및 부하 테스트.
3. **무선/이더넷 통합**: 최종 안정성 검증.

### ⚠️ 현재 상태
- `detectLCD()`에서 `FF FF FF` 응답 발생 중. 하드웨어 신호 분석 대기 상태.

