# 프로젝트 진행 상황

## 2026-04-03
### 하드웨어 레퍼런스 문서화 및 LCD 성능 최적화
*   **하드웨어 정보 통합**: `examples/tas/docs/hardware_reference.md`를 신규 생성하여 LilyGO T-ETH-Lite-S3 장비의 핀맵, LCD 사양, 버튼 및 주변기기 정보를 상세히 기록.
*   **LCD 통신 속도 향상**: `LGFX_Config.hpp`에서 16비트 병렬 버스 클럭을 5MHz에서 20MHz로 상향하여 데이터 전송 대역폭 확장 및 렌더링 지연 감소.
*   **UI 렌더링 성능 검증**: `tas.ino`에 LVGL 60Hz 타이머 기반의 테스트 사각형(`move_test_rect`) 애니메이션을 추가하여 실시간 화면 갱신 성능 및 드라이버 안정성 확인.
*   **배경 기능 복구**: `utilities.h`의 `ENABLE_GRADIENT_BG` 매크로를 활성화하여 대시보드의 정적인 가로 그라데이션 배경을 복원.
*   **렌더링 정책 수정**: `disp_drv.full_refresh = 1`로 설정하여 부분 업데이트 시 발생할 수 있는 티어링 현상을 방지하고 전체 화면 갱신 안정성을 우선시하도록 조정.

## 2026-04-01
### 하드웨어 빌드 안정화 및 UI 리팩토링
*   **부팅 루프 문제 해결**: `platformio.ini`의 `board_evb` 환경을 QIO/esp32s3box 모드로 강제 지정하여 DIO 부팅 모드로 인한 무한 재부팅(Panic) 문제를 완벽하게 해결.
*   **대시보드 UI 개편**: `tas.ino` 내의 구형 벤치마크 테스트 코드를 전면 제거하고, 상용 네트워크 장비 스타일에 맞춘 실시간 정보 패널(Ethernet / WiFi AP 상태 표시) 카드 UI 뷰로 리팩토링 완료.
*   **안전 커스텀 핀맵 설계**: 16비트 커스텀 LCD 모듈 장착용으로, ESP32-S3의 PSRAM 및 스트래핑 핀 충돌을 회피하는 21개의 안전 범용 GPIO 핀맵 배선 가이드를 수립하고 `platformio.ini`의 `[lcd3]` 옵션에 반영.
*   **디스플레이 구동 이슈 식별**: 현재 메인 코어인 `TFT_eSPI` 라이브러리가 ESP32-S3 칩셋 환경에서는 16비트 병렬 인터페이스(LCD_CAM)를 내부적으로 미지원하여 백화현상(SPI Fallback)이 발생함을 최종 원인 규명. (차후 LovyanGFX로 포팅 예정)

## 2026-03-31
### 빌드 시스템 정보 출력 강화 및 설정 최적화
*   **빌드 타임 로그 강화**: `tas.ino`에 `#pragma message`를 도입하여 빌드 시 보드 타입, LCD 드라이버, SPI 클럭, LVGL 타겟 FPS, 터치 활성화 여부가 터미널에 출력되도록 개선.
*   **매크로 출력 로직 수정**: 전처리기에서 수식이 계산되지 않고 문자열로 치환되던 문제(예: `40000000/1000000 MHz`)를 해결하기 위해 주요 값들에 대한 조건부 `#if` 분기 처리 적용.
*   **하드웨어 성능 최적화**: `platformio.ini`에서 `lcd2`(ILI9341) 환경의 SPI 클럭을 40MHz에서 80MHz로 상향 조정하여 다른 LCD 환경과 성능 수준 동기화.

### LCD 성능 최적화 및 안정성 개선 (60 FPS 복구)
*   **성능 병목 해결**: `ENABLE_TE_SYNC` 비활성화를 통해 하드웨어 TE 신호 대기 타임아웃(100ms)으로 인한 8 FPS 저하 문제를 해결하고 실시간 성능 복구.
*   **코드 안정성 강화**: 
    *   `move_test_rect_cb` 및 `initLVGLUI` 내 `lv_scr_act()` 반환값에 대한 Null Pointer 체크를 추가하여 시스템 행(Hang) 방지.
    *   `ui_test_rect` 생성 시 잘못된 스타일 설정(`0` 전달) 버그 수정 및 유효성 검사 로직 추가.
    *   그라데이션 애니메이션 타이머 주기를 `LV_DISP_DEF_REFR_PERIOD` 설정값과 동기화.
*   **하드웨어 설정 최적화**:
    *   `Setup217` 설정 파일 내 드라이버를 `ST7789_DRIVER`로 확정하고, `platformio.ini`의 80MHz 클럭이 우선 적용되도록 중복 코드 정리.
    *   `disp_drv.full_refresh = 1` 활성화를 통해 화면 전체 갱신 안정성 확보.

### 하드웨어 빌드 시스템 모듈화 완료
*   **멀티 보드/LCD 매트릭스 구축**: `platformio.ini`를 개편하여 3종의 보드(`lilygo`, `evb`, `tas`)와 3종의 LCD(`lcd1`, `lcd2`, `lcd3`) 조합으로 총 9개의 빌드 환경을 자동 생성하도록 구성.
*   **터치 드라이버 추상화**: `tas.ino` 내에서 `#ifdef` 분기 처리를 통해 `XPT2046`(SPI)과 `GT911`(I2C) 터치 드라이버를 하드웨어 사양에 맞춰 동적으로 초기화 및 작동하도록 개선.
*   **빌드 오류 해결 및 안정화**: 
    *   ESP32-S3 플랫폼의 `digitalPinToInterrupt` 매크로 부재 문제를 해결하기 위해 `include/esp32_fix.h` 전역 적용.
    *   `TAMC_GT911` 라이브러리의 초기화 파라미터 오류 수정 및 중복 코드 정리.
    *   터치 패널이 없는 환경(`lcd3`)에서도 런타임 에러 없이 빌드 및 동작하도록 로직 보완.
*   **전체 환경 검증**: 9개 모든 환경(`lilygo-lcd1~3`, `evb-lcd1~3`, `tas-lcd1~3`)에 대해 `platformio run` 테스트를 통과하여 빌드 안정성 확보.

### ENABLE_TOUCH 매크로 도입 및 터치 구성 최적화
*   **터치 기능 옵션화**: `ENABLE_TOUCH` 매크로를 도입하여 터치 드라이버 라이브러리 포함, 인스턴스 생성, LVGL 입력 장치 등록 및 콜백 로직을 조건부 컴파일하도록 개선.
*   **하드웨어별 구성 분리**:
    *   `platformio.ini`의 `lcd1` (ST7789) 환경에 `ENABLE_TOUCH` 활성화.
    *   `lcd2` (ILI9341) 환경에서 터치 기능을 선택적으로 비활성화하도록 설정.
*   **코드 구조 개선**: 터치 관련 전역 변수 및 UI 요소(커서, 십자선 등)를 매크로 블록으로 격리하여 메모리 및 실행 효율 최적화.
*   **빌드 검증**: `lilygo-lcd1`(터치 활성) 및 `lilygo-lcd2`(터치 비활성) 환경 모두 빌드 성공 및 정상 동작 확인.

## 2026-03-26
### 2026-03-26 (오후): GT911 터치 인터페이스 및 정밀 보정 완료
*   **터치 컨트롤러 통합**: GT911 (I2C) 드라이버 연동 및 `platformio.ini` 환경 설정 (IO17, 18, 21, 38).
*   **좌표 정밀 매핑**: 4개 모서리 캘리브레이션 데이터를 기반으로 X(0~300 -> 0~319), Y(85~315 -> 0~239) 픽셀 매핑 및 오프셋 보정 완료.
*   **디버깅 UI 구현**: Android 스타일의 십자선(Crosshair), 실시간 좌표 정보 레이블을 `lv_layer_sys()` 최상위 레이어에 구현하여 가시성 확보.
*   **배경 애니메이션 최적화**: `ENABLE_GRADIENT_BG` 매크로를 통해 배경 색상 변경 및 그라데이션 애니메이션을 선택적으로 온/오프할 수 있도록 구조화 (기본 OFF).
- **LCD 멀티태스킹 및 성능 최적화 보완**:
    - `my_disp_flush` 내부에 `yield()`를 추가하여 DMA 전송 대기 중 타 태스크(이더넷/WiFi)의 실행 기회 확보.
    - SPI 클럭을 80MHz로 상향하고 `LV_DISP_DEF_REFR_PERIOD`를 16ms로 설정하여 60 FPS 타겟 성능 최적화.
- **UI 및 동기화 로직 대규모 정비**:
    - 불필요한 이더넷/AP 상태 라벨을 숨김 처리하여 화면 렌더링 부하 경감.
    - `ENABLE_TE_SYNC` 매크로를 통한 TE(Tearing Effect) 동기화 모드와 고속(Fast) 모드 선택 로직 분리 및 정교화.
    - 디스플레이 하드웨어 프레임 레이트(`FRCTRL2`)를 기본값인 60Hz로 명시적 설정.
    - 고속 모드(Fast Mode)에서 `tft.initDMA()`를 명시적으로 호출하여 비동기 전송 안정화.
    - UI 상단에 TE 신호의 주기 및 주파수(Hz)를 실시간으로 모니터링하는 라벨 추가.

## 2026-03-25
- **LCD TE(Tearing Effect) 핀 및 V-Sync 동기화 한계 검증**:
    - ST7789 TE 신호 활성화 커맨드(`0x35`) 추가 및 GPIO 21번을 하드웨어 인터럽트(ISR)로 연결.
    - 플로팅 노이즈(가짜 인터럽트) 차단을 위해 `INPUT_PULLDOWN` 설정 적용 및 오실로스코프 파형 측정 안내.
    - 부분 버퍼(64라인) 렌더링 방식에서의 스캐너 역전 현상 분석.
    - 프레임 하락을 막고 50+ FPS의 최대 성능을 확보하기 위해 논블로킹(TE 대기 해제) 전송 유지.
- **LVGL 렌더링 소요 시간 실시간 모니터링 적용**:
    - `disp_drv.monitor_cb` 콜백을 이용해 프레임당 순수 렌더링 소요 시간을 측정하는 `my_monitor_cb` 구현.
    - 우측 상단 UI 라벨에 FPS 숫자와 함께 소요 시간(`ms`)이 실시간 표기되도록 개선.

## 2026-03-20
- **LCD 디버깅을 위한 이더넷 격리**:
    - `utilities.h`에 `ENABLE_ETHERNET` 토글 추가 (충돌 방지를 위해 기본적으로 비활성화).
    - `tas.ino` 내의 이더넷 초기화, 이벤트 핸들러, 루프 로직을 `#ifdef ENABLE_ETHERNET`으로 감싸 처리.
    - `tas.ino` 내의 이더넷 관련 셸 명령어(`stats`, `iperf`, `dhcp` 등)를 격리.
- **빌드 및 호환성 수정**:
    - `platformio.ini`의 소스 필터를 수정하여 모든 소스 파일(`+<*>`)이 포함되도록 수정.
    - `lib_ignore`에서 누락되었던 필수 라이브러리(`ETHClass2`, `ESP32Ping`, `lvgl`) 복구.
    - `tas.ino` 내의 Arduino Core 2.0.x 호환성 문제 해결 (`WiFi.AP.netif()` 관련).
    - LCD 타이틀 폰트를 `lv_font_montserrat_14`로 변경하여 링크 에러 해결.
- **문서화 및 워크플로우**:
    - 상세한 SPI 채널 분석 및 설정 내용을 담은 `docs/spi.md` 생성.
    - LCD 구현 진행 상황을 추적하기 위해 `docs/todo.md` 업데이트.
    - 자동화된 문서화 및 릴리스를 위해 `/commit` 워크플로우를 등록 및 실행.
    - 개발 시작 전 문맥 파악을 위한 `/load` 워크플로우 등록 및 `load.md` 작성.
- **워크플로우 가이드라인 강화**:
    - 모든 문서 업데이트 시 **한글**- [x] 프로젝트 변경 사항 커밋 `@[/commit]`
    - [x] 변경 사항 분석 (`git status`, `git diff`)
    - [x] `progress.md` 업데이트 (한글)
    - [x] 스테이징 및 커밋 (`git add`, `git commit`)
    - [x] 원격 저장소 푸시 (`git push`)
- [x] FPS 성능 개선 (SRAM DMA 더블 버퍼링 적용)
    - [x] SRAM/PSRAM 싱글 버퍼 테스트 완료
    - [x] 80MHz SPI 클럭 설정 완료 (25 FPS 확인)
    - [x] SRAM DMA 더블 버퍼(160라인 x 2) 코드 작성 및 업로드
    - [x] 결과 확인 (30 FPS 도달 기대)
- `utilities.h`에 `ENABLE_LCD` 매크로 정의 및 관련 기능 활성화.
- **하드웨어 디버깅 계획 수립**:
    - `todo.md`에 오실로스코프를 이용한 SPI 신호(SCLK, MOSI, MISO, RST, CS) 정밀 분석 계획 반영.

## 2026-03-23
- **LCD SPI 클럭 및 하드웨어 감지 이슈 해결**:
    - **SPI 버스 충돌 해결**: 기본 SPI 객체가 이더넷 모듈과 동일한 SPI3_HOST를 공유하여 발생하는 충돌을 확인하고, LCD용 버스를 **SPI2_HOST (FSPI)**로 명시적 분리하여 클럭 신호 누락 문제 해결.
    - **더미 바이트 보정**: ST7789V 데이터 시트 분석을 통해 `0x04`, `0xDA/B/C` 명령어 사용 시 필수적인 **1바이트 더미 읽기(Dummy read)** 로직 적용.
    #### 1. 주요 작업 내용
*   **LCD 성능 최적화 실험**:
    *   **SPI 클럭**: 40MHz -> 80MHz 상향 성공.
    *   **버퍼 전략 최적화**:
        *   **SRAM Single (20~240 line)**: 최대 20 FPS (Blocking).
        *   **PSRAM Single (Full)**: 80MHz 시 최대 25-26 FPS (Blocking).
        *   **SRAM DMA Double (160 line x 2)**: **최종 30 FPS 달성 목표 적용 완료**.
    *   **메모리 최적화**: `MALLOC_CAP_DMA`를 통한 내부 SRAM 효율적 활용 (81% 사용).
- **하드웨어 감지 성공**: `RDID2/3` 명령어를 통해 유효한 디지털 패턴(`F0`, `0F`)을 검출하여 LCD 하드웨어 존재 증명 및 화면 출력 성공 확인.
- **로우레벨 디버깅 및 분석**:
    - GPIO Matrix 및 IO_MUX 레지스터 덤프 기능을 구현하여 핀 매핑 및 내부 시그널(101 vs 63) 충돌 원인 규명.
    - 비트뱅잉(Bit-banging) 테스트를 통해 더미 비트 가설 검증 및 칩 특성 분석.
- **LCD 색상 및 성능 검증 및 최적화**:
    - 실시간 FPS 측정을 위한 카운터 및 UI 라벨 추가.
    - 색상 정확도 검증을 위한 R, G, B, W, K 테스트 팔레트 구현.
    - **색상 및 텍스트 왜곡 해결**:
        - `platformio.ini`에 `TFT_RGB_ORDER=TFT_BGR` 추가 (Red-Blue 스왑 해결).
        - `LV_COLOR_16_SWAP=1` 및 `pushColors(..., false)` 설정을 통해 엔디안(Endian) mismatch로 인한 글자 깨짐 및 3방향 색상 밀림 현상 완전 해결.
- **문서화 및 가이드라인**:
    - LCD 구동 분석 및 최적 설정 가이드를 정리한 `docs/lcd.md` 생성 및 `docs` 폴더로 이동.
- **LCD 성능 극대화 및 안정화**:
    - **더블 버퍼링(Double Buffering) 및 DMA 적용**: 320라인 싱글 버퍼를 160라인 x 2 분할 버퍼로 재구성하고 SPI DMA를 활성화하여, 렌더링과 전송을 병렬로 처리함에 따라 화면 훑음(Scanline) 현상을 제거하고 시각적 부드러움 확보.
    - **클럭 최적화**: CPU 클럭을 240MHz(S3 최대)로, SPI 클럭을 80MHz(하드웨어 한계치)로 상향하여 전체 화면 업데이트에서도 안정적인 30 FPS를 유지하도록 최적화.
    - **프레임 레이트 정확도 개선**: 단순 전송 횟수(Partial flush)가 아닌 LVGL 내부의 평균 논리 프레임(`lv_refr_get_fps_avg`) 측정 방식으로 전환하여 실제 체감 성능을 정확히 표기하도록 수정.
    - **설정 병목 해결**: `lv_conf.h`의 주사율 제한(50ms) 설정을 30 FPS 타겟(33ms)에 맞게 정렬하여 소프트웨어적 제약 해소.
- **문서화 최종화**: `docs/lcd.md`와 `docs/todo.md`에 최종 하드웨어 설정값, 성능 지표, 그리고 더블 버퍼링 구현 세부 사항을 최신화하여 운영 가이드 완성.

## 2026-03-24
- **LCD DMA 및 더블 버퍼링 원복**:
    - Non-blocking 전송(DMA) 시 화면 업데이트가 상시적으로 발생하지 않는 문제를 해결하기 위해 이전의 안정적인 싱글 버퍼(20라인) 및 블로킹 전송 방식으로 원복.
    - `tas.ino`: 160라인 x 2 더블 버퍼를 20라인 싱글 버퍼로 변경하여 동기식 SPI 전송 안정성 확보.
    - `tas.ino`: `pushImageDMA` 및 `dmaWait` 코드로 인한 비동기 충돌 가능성을 제거하고 `pushColors` 기반의 블로킹 전송으로 복구.
    - `tas.ino`: `tft.initDMA()` 호출을 제거하여 하드웨어 리소스 최적화.
    - **LCD 성능 극대화 (18 FPS -> 53 FPS / 300% 향상)**:
    - SPI 클럭 80MHz 상향 및 비동기 DMA(`tft.endWrite()` 제거) 적용으로 병렬 처리 극대화.
    - ESP32-S3의 32KB L1 캐시에 맞춘 **64라인 더블 버퍼링**으로 CPU 연산 효율 100% 달성.
    - `lv_conf.h` 가드 추가 및 `LV_DISP_DEF_REFR_PERIOD=16` 설정으로 소프트웨어 주사율 제한 해제.
    - 40MHz vs 80MHz 비교 벤치마크 수행으로 성능 병목 구간(SPI 대역폭) 확인.
    - 최종 성능: **53 FPS (CPU 82%)**의 안정적인 고주사율 환경 구축 완료.
